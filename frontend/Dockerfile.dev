FROM node:20-slim

WORKDIR /app

# Copy source code and host node_modules
COPY . .

# Expose port
EXPOSE 5173

# Start Vite dev server binding to 0.0.0.0
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
